import { Divider } from '@mui/material';
import { FC } from 'react';

const FooterDivider: FC = () => (
  <Divider orientation="vertical" color="light" sx={{ minHeight: 25 }} />
);

export default FooterDivider;
