import { SharedLogic } from '@interfaces/cardOptions';

export const defaultSharedLogic: Required<SharedLogic> = {
  hasCardInfo: true,
  hasIllustratorName: true,
  hasDexStats: false,
  hasDexEntry: false,
};
